
spring:
  jpa:
    #    show-sql: true
    generate-ddl: true
    hibernate.ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob:
            non_contextual_creation: true

  datasource:
    url: "jdbc:h2:~/webforum;DB_CLOSE_DELAY=-1;MODE=MYSQL;INIT=CREATE SCHEMA IF NOT EXISTS webforum"
    driver-class-name: org.h2.Driver
    username: "sa"
    password: ''
